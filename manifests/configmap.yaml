apiVersion: v1
kind: ConfigMap
metadata:
  name: taskapp-config
  namespace: taskapp
data:
  FLASK_APP: "run.py"
  FLASK_ENV: "production"
  LOG_LEVEL: "INFO"
  ALLOWED_HOSTS: "taskapp.local,api.local"
